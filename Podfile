# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'DesafioiOS' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for DesafioiOS
  pod 'SwiftGen', '6.5.1'

  target 'DesafioiOSTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
